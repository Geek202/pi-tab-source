###############################
#                             #
# This is open souce software #
#                             #
###############################

Instructions to install:

Use git to clone the repo https://github.com/Geek202/pi-tab

Move into the folder with:
$ cd pi-tab

run the command:
$ sudo dpkg -i pi-tab.deb

This will instruact the dpkg (the Debian package installer) to install the package.
